
// delete this this was just to test setup and doesn't test anything in the code
import { describe, test, expect } from 'vitest';

describe('Login Tests', () => {
  test('should pass a basic test', () => {
    expect(1 + 1).toBe(2);
  });
});